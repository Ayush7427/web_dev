const arry  = [1,2,3,4,5]
let str = ""

for(let i = 0; i <= arry.length; i++){
    if(arry % 3 == 0) {
        str  = str + "divisible"
    }
    else {
        str  = str + "divisible"
    }
}
console.log(str);